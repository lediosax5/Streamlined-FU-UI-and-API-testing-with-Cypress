{  
  "scripts": {
  "sample": "echo \"Error: no test specified\" && exit 1",
  "chr": "npx cypress open --e2e -b chrome",
  "ele": "npx cypress open --e2e -b electron"
},
  "devDependencies": {
    "cypress": "^13.11.0",
    "cypress-xpath": "^2.0.1"
  }
}
